apiVersion: v1
kind: Service
metadata:
  name: ExternalIntegration
  labels:
    app: ExternalIntegration
spec:
  ports:
    - port: 8080
      targetPort: 8080
  selector:
    app: ExternalIntegration